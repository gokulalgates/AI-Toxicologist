gradio>=4.0.0
pandas>=2.0.0
pubchempy>=1.0.4
biopython>=1.81
langchain>=0.1.0
langchain-ollama>=0.1.0
ollama>=0.1.0  # For direct Ollama API access (used by agent RAG system)
matplotlib>=3.7.0
seaborn>=0.12.0
networkx>=3.1
pydantic>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
requests>=2.28.0  # For full-text retrieval APIs
pdfplumber>=0.10.0 # For parsing PDFs from full-text retrieval
beautifulsoup4>=4.12.0  # Optional: For parsing HTML to find PDF links (falls back gracefully if not available)
pybliometrics>=3.0.0  # Optional: For enhanced PDF downloads via Scopus API (requires API key in ~/.scopus/config.ini)
pytest>=7.0.0  # For running tests
mpi4py>=3.1.0  # Optional: For MPI distributed computing support
sentence-transformers>=2.2.0  # Optional: For RAG embeddings (falls back to simple similarity if not available)
python-pptx>=0.6.0  # Optional: For creating PowerPoint presentations
